{% extends 'base.html.twig' %}

{% block title %}Messages{% endblock %}

{% block body %}
    <h1 class="title">Messages</h1>

    {# <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Title</th>
                <th>Message</th>
                <th>Email</th>
                <th>Action</th>
            </tr>
        </thead>
        <tbody>
        {% for contact in contacts %}
            <tr>
                <td>{{ contact.id }}</td>
                <td>{{ contact.title }}</td>
                <td>{{ contact.message }}</td>
                <td>{{ contact.email }}</td>
                <td>{{ include('contact/_delete_form.html.twig') }}</td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table> #}

    {% if contacts %}
        <div class="sourceIndex">
            <ul>
                {% for contact in contacts %}
                    <li class="sourceElementIndex">
                        <div>
                            <h2 class="title">Objet :</h2> <p>{{contact.title}}</p>
                        </div>
                        <div>
                            <h2 class="title">Message :</h2> <p class="messageContact">{{contact.message}}</p>
                        </div>
                        <div>
                            <h2 class="title">Contact :</h2> <p>{{contact.email}}</p>
                        </div>
                        <div class="editSourceButton">
                            {{ include('contact/_delete_form.html.twig') }}
                        </div>
                    </li>
                {% endfor %}
            </ul>
        </div>
    {% else %}
        <p>Aucun message</p>
    {% endif %}

    <div class="buttonsNav">
        <a href="{{ path('admin') }}">Accueil d'administration</a>
    </div>
{% endblock %}
