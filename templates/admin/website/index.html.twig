{% extends 'base.html.twig' %}

{% block title %}Mon site{% endblock %}

{% block body %}
    <h1 class="title">Mon site</h1>

    {% if website %}
        {# Afficher le mode édition directemenent #}
        {{ include('admin/website/_form.html.twig', {'button_label': 'Enregistrer les modifications'}) }}
        {{ include('admin/website/_delete_form.html.twig') }}
    {% else %}
        <p>Votre portfolio est vierge</p>
        <div class="buttonsNav">
            <a href="{{ path('website_new') }}">Créer un portfolio</a>
        </div>
    {% endif %}
    <div class="buttonsNav">
        <a href="{{ path('admin') }}">Retour à l'accueil</a>
    </div>
{% endblock %}
