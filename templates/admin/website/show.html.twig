{% extends 'base.html.twig' %}

{% block title %}Website{% endblock %}

{% block body %}
    <h1>Website</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ website.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ website.title }}</td>
            </tr>
            <tr>
                <th>Biglogo</th>
                <td>{{ website.biglogo }}</td>
            </tr>
            <tr>
                <th>Littlelogo</th>
                <td>{{ website.littlelogo }}</td>
            </tr>
            <tr>
                <th>Typeportfolio</th>
                <td>{{ website.typeportfolio }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_website_index') }}">back to list</a>

    <a href="{{ path('app_website_edit', {'id': website.id}) }}">edit</a>

    {{ include('website/_delete_form.html.twig') }}
{% endblock %}
